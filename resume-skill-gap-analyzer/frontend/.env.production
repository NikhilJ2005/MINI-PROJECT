# Production: frontend and backend are served from the same origin (single container)
# Leave empty so API calls use relative paths (e.g., /analyze instead of http://localhost:8000/analyze)
VITE_API_BASE_URL=
